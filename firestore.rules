rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DEV ONLY — thay bằng rules chặt trước khi deploy production
      allow read, write: if true;
    }
  }
}
